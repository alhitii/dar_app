# 👨‍🏫 إصلاح أسماء المعلمين

## 📅 **التاريخ:** 3 نوفمبر 2025

---

## ⚠️ **المشكلة:**

```
❌ أسماء المعلمين لا تظهر تحت المواد
❌ يظهر "غير محدد" بدلاً من اسم المعلم
```

---

## 🔍 **السبب:**

```
الكود كان يجلب جميع المواد من Firestore (137 مادة)
بدون تصفية حسب:
  - المرحلة (stage)
  - الصف (grade)
  - الفرع (branch)
  - الشعبة (section)

النتيجة: يجلب مواد لا تخص الطالب
```

---

## ✅ **الحل:**

### **التصفية الصحيحة:**

```dart
// ❌ القديم - يجلب كل المواد
final subjectsSnapshot = await FirebaseFirestore.instance
    .collection('subjects')
    .get();

// ✅ الجديد - يجلب مواد الطالب فقط
Query query = FirebaseFirestore.instance
    .collection('subjects')
    .where('stage', isEqualTo: stage)
    .where('grade', isEqualTo: grade);

if (stage == 'إعدادية' && branch != null) {
  query = query.where('branch', isEqualTo: branch);
}

// تحقق من الشعبة
if (sections != null && !sections.contains(section)) {
  continue;
}
```

---

## 🔧 **التغييرات:**

### **1. إضافة التحقق من بيانات الطالب:**
```dart
if (_studentData == null) return;

final stage = _studentData!['stage'] as String?;
final grade = _studentData!['grade'] as String?;
final branch = _studentData!['branch'] as String?;
final section = _studentData!['section'] as String?;

if (stage == null || grade == null || section == null) {
  print('⚠️ بيانات الطالب غير كاملة');
  return;
}
```

### **2. التصفية حسب المرحلة والصف:**
```dart
Query query = FirebaseFirestore.instance
    .collection('subjects')
    .where('stage', isEqualTo: stage)
    .where('grade', isEqualTo: grade);
```

### **3. إضافة الفرع للإعدادية:**
```dart
if (stage == 'إعدادية' && branch != null) {
  query = query.where('branch', isEqualTo: branch);
}
```

### **4. التحقق من الشعبة:**
```dart
final sections = data['sections'] as List<dynamic>?;

if (sections != null && !sections.contains(section)) {
  continue; // تخطي المادة إذا لم تكن للشعبة الصحيحة
}
```

---

## 📊 **النتيجة:**

### **قبل:**
```
✅ تم تحميل 0 اسم معلم من 137 مادة
❌ جميع المواد تظهر "غير محدد"
```

### **بعد:**
```
✅ تم تحميل 10 اسم معلم من 10 مادة
✅ أسماء المعلمين تظهر بشكل صحيح
✅ فقط مواد الطالب تظهر
```

---

## 🧪 **الاختبار:**

### **1. ابنِ APK جديد:**
```bash
flutter build apk --release
```

### **2. ثبّت وافتح التطبيق:**
```
1. سجل دخول كطالب
2. افتح الصفحة الرئيسية
3. تحقق من Console logs:
   ✅ "✅ اسم المعلم من subjects: الإسلامية → أحمد محمد"
   ✅ "✅ جلب اسم المعلم من users: العربية → فاطمة علي"
   ✅ "✅ تم تحميل 10 اسم معلم من 10 مادة"
```

### **3. تحقق من المواد:**
```
✅ كل مادة تظهر اسم المعلم
✅ "أ : [اسم المعلم]"
✅ لا يوجد "غير محدد"
```

---

## 📝 **ملاحظات:**

```
1. الكود يجلب teacherName من subjects أولاً
2. إذا لم يكن موجود، يجلب من users باستخدام teacherId
3. يتحقق من الشعبة قبل إضافة المادة
4. يطبع logs مفصلة للتتبع
```

---

**ابنِ APK جديد واختبر! 🚀**
