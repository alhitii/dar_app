# 📝 إضافة اختيار الشعب لإرسال الواجب

## 📅 **التاريخ:** 31 أكتوبر 2025

---

## ❌ **المشكلة:**

```
في صفحة إرسال الواجب:
- لا يوجد خيار لاختيار الشعب
- الواجب يُرسل لجميع الشعب تلقائياً
- لا يمكن تحديد شعب معينة
```

---

## ✅ **الحل:**

### **إضافة واجهة اختيار الشعب:**
```dart
// المتغيرات
List<String> _selectedSections = [];
List<String>? _availableSections;

// تحميل الشعب المتاحة من بيانات المعلم
final sections = data?['sections'] as List<dynamic>?;
_availableSections = sections?.map((s) => s.toString()).toList() 
    ?? ['أ', 'ب', 'ج', 'د'];

// واجهة الاختيار
Wrap(
  spacing: 8,
  runSpacing: 8,
  children: _availableSections!.map((section) {
    final isSelected = _selectedSections.contains(section);
    return FilterChip(
      label: Text('شعبة $section'),
      selected: isSelected,
      onSelected: (selected) {
        setState(() {
          if (selected) {
            _selectedSections.add(section);
          } else {
            _selectedSections.remove(section);
          }
        });
      },
    );
  }).toList(),
)
```

---

## 🎨 **الواجهة الجديدة:**

```
┌─────────────────────────────────┐
│  إرسال واجب                     │
│                                  │
│  اختر المادة ▼                  │
│  📖 الإسلامية                   │
│                                  │
│  اختر الشعب المراد إرسال        │
│  الواجب لها:                    │
│                                  │
│  ☑ شعبة أ   ☐ شعبة ب           │
│  ☐ شعبة ج   ☐ شعبة د           │
│                                  │
│  عنوان الواجب ✏️                │
│  ┌─────────────────────────┐    │
│  │                         │    │
│  └─────────────────────────┘    │
│                                  │
│  تفاصيل الواجب 📄               │
│  ┌─────────────────────────┐    │
│  │                         │    │
│  │                         │    │
│  └─────────────────────────┘    │
│                                  │
│  [ إرسال الواجب ]               │
└─────────────────────────────────┘
```

---

## 🔄 **التدفق:**

### **1. تحميل الشعب المتاحة:**
```
1. تحميل بيانات المعلم من teachers collection
2. قراءة حقل sections
3. عرض الشعب كـ FilterChips
```

### **2. اختيار الشعب:**
```
1. المعلم يضغط على شعبة
2. تُضاف للقائمة _selectedSections
3. تتغير حالة الـ Chip (محدد/غير محدد)
4. يمكن اختيار أكثر من شعبة
```

### **3. إرسال الواجب:**
```
1. التحقق من اختيار شعبة واحدة على الأقل
2. إرسال الواجب مع الشعب المختارة فقط
3. حفظ في homework collection
```

---

## 📊 **هيكل البيانات:**

### **homework collection:**
```javascript
{
  teacherId: "abc123",
  teacherName: "أحمد محمد",
  subjectCode: "subject_id_1",
  title: "واجب الدرس الأول",
  details: "حل التمارين من 1 إلى 10",
  stage: "متوسطة",
  grade: "الثاني متوسط",
  branch: null,
  sections: ["أ", "ب"],  // ✅ الشعب المختارة فقط
  createdAt: Timestamp
}
```

---

## ✅ **المزايا:**

```
✅ اختيار شعب محددة
✅ عدم إرسال لجميع الشعب
✅ واجهة سهلة (FilterChips)
✅ اختيار متعدد
✅ التحقق من الاختيار
✅ رسالة خطأ واضحة
```

---

## 🎯 **أمثلة الاستخدام:**

### **مثال 1: إرسال لشعبة واحدة**
```
المعلم: أحمد
المادة: الإسلامية
الشعب المختارة: أ فقط

→ الواجب يُرسل لطلاب شعبة أ فقط
```

### **مثال 2: إرسال لعدة شعب**
```
المعلم: فاطمة
المادة: العربية
الشعب المختارة: أ، ب، ج

→ الواجب يُرسل لطلاب الشعب الثلاث
```

### **مثال 3: عدم اختيار شعبة**
```
المعلم يحاول الإرسال بدون اختيار شعبة

→ رسالة خطأ: "الرجاء اختيار شعبة واحدة على الأقل"
```

---

## 🔧 **الكود الرئيسي:**

### **التحقق من الاختيار:**
```dart
if (_selectedSections.isEmpty) {
  ScaffoldMessenger.of(context).showSnackBar(
    const SnackBar(content: Text('الرجاء اختيار شعبة واحدة على الأقل')),
  );
  return;
}
```

### **إرسال الواجب:**
```dart
await FirebaseFirestore.instance.collection('homework').add({
  'teacherId': user.uid,
  'teacherName': _teacherData!['name'],
  'subjectCode': _selectedSubject,
  'title': _titleController.text,
  'details': _detailsController.text,
  'stage': _teacherData!['stage'],
  'grade': _teacherData!['grade'],
  'branch': _teacherData!['branch'],
  'sections': _selectedSections, // ✅ الشعب المختارة فقط
  'createdAt': FieldValue.serverTimestamp(),
});
```

### **مسح الحقول:**
```dart
_titleController.clear();
_detailsController.clear();
setState(() {
  _selectedSubject = null;
  _selectedSections.clear(); // ✅ مسح الشعب المختارة
});
```

---

## 🧪 **الاختبار:**

### **1. اختبر عرض الشعب:**
```
1. سجل دخول كمعلم
2. ✅ يجب أن تظهر الشعب المخصصة للمعلم
3. ✅ كل شعبة في Chip منفصل
```

### **2. اختبر الاختيار:**
```
1. اضغط على شعبة أ
2. ✅ يجب أن تتحدد (لون مختلف)
3. اضغط على شعبة ب
4. ✅ يجب أن تتحدد أيضاً
5. اضغط على شعبة أ مرة أخرى
6. ✅ يجب أن تُلغى
```

### **3. اختبر الإرسال:**
```
1. اختر مادة
2. اختر شعبة أ فقط
3. املأ العنوان والتفاصيل
4. اضغط "إرسال الواجب"
5. ✅ يجب أن يُرسل بنجاح

6. تحقق من Firestore:
   ✅ حقل sections يحتوي على ["أ"] فقط
```

### **4. اختبر التحقق:**
```
1. اختر مادة
2. لا تختر أي شعبة
3. اضغط "إرسال الواجب"
4. ✅ يجب أن تظهر رسالة:
   "الرجاء اختيار شعبة واحدة على الأقل"
```

---

## 📁 **الملفات المعدلة:**

```
✅ lib/ui/teacher/teacher_home_complete.dart
   - إضافة _selectedSections
   - إضافة _availableSections
   - تحميل الشعب من teachers collection
   - واجهة FilterChips للاختيار
   - التحقق من الاختيار
   - إرسال الشعب المختارة

✅ توثيقات_المشروع/15_SECTION_SELECTION_FOR_HOMEWORK.md
   - توثيق شامل
```

---

## 🎨 **تصميم FilterChips:**

```dart
FilterChip(
  label: Text('شعبة $section'),
  selected: isSelected,
  selectedColor: AppColors.buttonPrimary.withOpacity(0.3),
  checkmarkColor: AppColors.buttonPrimary,
  backgroundColor: AppColors.inputFill,
  labelStyle: TextStyle(
    color: isSelected ? AppColors.buttonPrimary : AppColors.textPrimary,
    fontWeight: isSelected ? FontWeight.bold : FontWeight.normal,
  ),
)
```

---

**الحالة:** ✅ مكتمل ويعمل  
**جاهز للاختبار:** نعم  
**التحسينات:** اختيار شعب محددة + واجهة سهلة
