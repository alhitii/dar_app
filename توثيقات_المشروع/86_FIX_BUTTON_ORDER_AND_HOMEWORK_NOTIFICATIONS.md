# إصلاح: ترتيب الأزرار وإشعارات الواجب

**التاريخ:** 6 نوفمبر 2025  
**الحالة:** ✅ جزئي (الأزرار تم إصلاحها، الإشعارات تحتاج فحص)
**الإصدار:** v1.0.0+1

---

## 🔍 المشاكل المكتشفة

### 1. ترتيب الأزرار ❌ → ✅ تم الإصلاح

**المشكلة:** 
- كان الترتيب في `student_home_complete.dart` (الصفحة الرئيسية للطالب):
  - يسار: لا شيء
  - وسط: العنوان
  - يمين: معلومات الحساب 👤 + الإشعارات 🔔

**الحل:**
- تم عكس ترتيب الأزرار:
  - يسار: الإشعارات 🔔 (وردي)
  - وسط: العنوان
  - يمين: معلومات الحساب 👤 (أزرق)

**الملف المعدل:**
- `lib/ui/student/student_home_complete.dart` - دالة `_buildHeader()`

---

### 2. إشعارات الواجب الجديد ⚠️

**التشخيص الأولي:**

النظام موجود ويعمل كالتالي:

#### ✅ **ما يعمل:**
1. **الاستماع للواجبات الجديدة:**
   - الكود يستمع لـ `notifications_homeworks` في Firestore
   - عند وجود واجب جديد، يعرض إشعار محلي مع صوت واهتزاز

2. **شارة "واجب جديد" على بطاقة المادة:**
   ```dart
   // شارة الواجب الجديد
   if (hasHomework && !_viewedHomeworks.contains(subjectName))
     Positioned(
       child: Text('واجب جديد', ...)
     )
   ```

3. **تبويب "الواجبات السابقة":**
   - يظهر في قائمة الإشعارات
   - يعرض جميع الواجبات المرسلة (خلال 7 أيام)

#### ❓ **ما يحتاج فحص:**

**السؤال:** أين يتوقع المستخدم رؤية "تنبيه واجب جديد"؟

**الاحتمالات:**

1. **في الصفحة الرئيسية** (موجود ✅):
   - شارة "واجب جديد" تظهر على بطاقة المادة
   - الشارة تختفي بعد فتح بطاقة المادة

2. **في قائمة الإشعارات** (موجود جزئياً):
   - تبويب "الواجبات السابقة" يعرض جميع الواجبات
   - **لكن لا يوجد تبويب "واجبات جديدة" منفصل**

3. **كإشعار push** (موجود ✅):
   - الإشعار المحلي يظهر مع صوت واهتزاز
   - يحتاج فحص إذا كان Firebase Cloud Messaging مفعّل

---

## 🔧 الإصلاحات المطبقة

### ✅ 1. عكس ترتيب الأزرار

**قبل:**
```dart
children: [
  // العنوان
  Expanded(...),
  // زر معلومات الحساب
  Container(...),
  const SizedBox(width: 8),
  // زر الإشعارات
  Container(...),
]
```

**بعد:**
```dart
children: [
  // زر الإشعارات
  Container(...),
  // العنوان
  Expanded(...),
  // زر معلومات الحساب
  Container(...),
]
```

---

## 🧪 الفحص المطلوب

### اختبار 1: شارة "واجب جديد" على بطاقة المادة

**الخطوات:**
1. افتح التطبيق كطالب
2. اطلب من المعلم إرسال واجب جديد
3. ✅ **يجب أن تظهر شارة "واجب جديد" على بطاقة المادة**
4. اضغط على بطاقة المادة
5. ✅ **يجب أن تختفي الشارة بعد الفتح**

### اختبار 2: تبويب الواجبات في قائمة الإشعارات

**الخطوات:**
1. افتح التطبيق كطالب
2. اضغط على زر الإشعارات 🔔
3. اذهب لتبويب "الواجبات السابقة"
4. ✅ **يجب أن تظهر جميع الواجبات المرسلة**

### اختبار 3: الإشعار المحلي

**الخطوات:**
1. أغلق التطبيق أو ضعه في الخلفية
2. اطلب من المعلم إرسال واجب جديد
3. ✅ **يجب أن يظهر إشعار محلي مع صوت واهتزاز**

---

## 💡 الحلول المحتملة (إذا لم تظهر الإشعارات)

### الحل 1: إضافة تبويب "واجبات جديدة" منفصل

في قائمة الإشعارات، يمكن إضافة تبويب رابع:
- **الواجبات الجديدة** (خلال 24 ساعة)
- الواجبات السابقة (7 أيام)
- إشعارات الإدارة
- سجل الغيابات

### الحل 2: إضافة Badge على زر الإشعارات

عرض عدد الواجبات الجديدة كـ Badge على زر الإشعارات:
```dart
Stack(
  children: [
    IconButton(
      icon: Icon(Icons.notifications),
      ...
    ),
    if (newHomeworksCount > 0)
      Positioned(
        right: 0,
        top: 0,
        child: Container(
          padding: EdgeInsets.all(2),
          decoration: BoxDecoration(
            color: Colors.red,
            shape: BoxShape.circle,
          ),
          child: Text('$newHomeworksCount'),
        ),
      ),
  ],
)
```

### الحل 3: التحقق من Firebase Cloud Messaging

تأكد من أن:
1. `firebase_messaging` مفعّل في `main.dart`
2. `onBackgroundMessage` يعمل بشكل صحيح
3. الـ token يتم حفظه في Firestore

---

## 📋 ملخص

| العنصر | الحالة | الملاحظة |
|--------|--------|----------|
| ترتيب الأزرار | ✅ تم الإصلاح | الإشعارات يسار، المعلومات يمين |
| شارة "واجب جديد" | ✅ موجود | على بطاقة المادة في الصفحة الرئيسية |
| تبويب الواجبات | ✅ موجود | في قائمة الإشعارات |
| الإشعار المحلي | ⚠️ يحتاج فحص | يجب التأكد من عمله |
| FCM Push | ⚠️ يحتاج فحص | يجب التأكد من التكوين |

---

## ✅ التوصيات

1. **اختبر التطبيق** وأخبرني بالنتيجة:
   - هل تظهر شارة "واجب جديد" على بطاقة المادة؟
   - هل يظهر الإشعار المحلي عند إرسال واجب جديد؟
   - أين بالضبط تريد رؤية "تنبيه واجب جديد"؟

2. **إذا لم تظهر الإشعارات:**
   - سأضيف تبويب "واجبات جديدة" منفصل
   - سأضيف Badge على زر الإشعارات
   - سأفحص إعدادات Firebase Cloud Messaging

3. **البناء:**
   - بعد التأكد من عمل كل شيء، نفّذ:
     ```bash
     flutter build windows --release
     flutter build apk --release
     ```
