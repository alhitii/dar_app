# 📚 إضافة تبويب الواجبات المرسلة للمعلم

## 📅 **التاريخ:** 31 أكتوبر 2025

---

## 🎯 **الهدف:**

إضافة تبويب في صفحة المعلم لعرض جميع الواجبات التي أرسلها مع إمكانية حذفها.

---

## ✅ **التعديلات:**

### **1️⃣ إضافة TabBar:**
```dart
TabBar(
  controller: _tabController,
  tabs: [
    Tab(icon: Icon(Icons.add_task), text: 'إرسال واجب'),
    Tab(icon: Icon(Icons.list_alt), text: 'الواجبات المرسلة'),
  ],
)
```

### **2️⃣ تبويب الواجبات المرسلة:**
```dart
Widget _buildSentHomeworkTab() {
  return StreamBuilder<QuerySnapshot>(
    stream: FirebaseFirestore.instance
        .collection('homework')
        .where('teacherId', isEqualTo: user?.uid)
        .orderBy('createdAt', descending: true)
        .snapshots(),
    builder: (context, snapshot) {
      // عرض الواجبات في ListView
    },
  );
}
```

---

## 🎨 **الواجهة:**

### **التبويبات:**
```
┌─────────────────────────────────┐
│  📝 إرسال واجب  |  📋 الواجبات المرسلة │
└─────────────────────────────────┘
```

### **تبويب الواجبات المرسلة:**
```
┌─────────────────────────────────┐
│  📚 واجب الدرس الأول            │
│  الصف: الثاني متوسط             │
│  الشعب: أ، ب                    │
│  2025/10/31                      │
│  [🗑️ حذف]                       │
├─────────────────────────────────┤
│  📚 واجب الدرس الثاني           │
│  الصف: الثاني متوسط             │
│  الشعب: أ                        │
│  2025/10/30                      │
│  [🗑️ حذف]                       │
└─────────────────────────────────┘
```

---

## 📊 **البيانات المعروضة:**

### **لكل واجب:**
```
✅ عنوان الواجب
✅ الصف
✅ الشعب المرسل لها
✅ تاريخ الإرسال
✅ زر حذف
```

---

## 🔄 **التدفق:**

### **1. عرض الواجبات:**
```
1. تحميل من Firestore
2. فلترة حسب teacherId
3. ترتيب حسب التاريخ (الأحدث أولاً)
4. عرض في ListView
```

### **2. حذف واجب:**
```
1. المعلم يضغط على زر الحذف
2. تظهر رسالة تأكيد
3. إذا وافق → حذف من Firestore
4. تظهر رسالة نجاح
```

---

## 🎯 **الميزات:**

```
✅ عرض جميع الواجبات المرسلة
✅ ترتيب حسب التاريخ (الأحدث أولاً)
✅ عرض التاريخ بحجم صغير ولون خافت
✅ إمكانية حذف الواجب
✅ رسالة تأكيد قبل الحذف
✅ تحديث تلقائي (StreamBuilder)
✅ رسالة عند عدم وجود واجبات
```

---

## 🔧 **الكود الرئيسي:**

### **Query الواجبات:**
```dart
FirebaseFirestore.instance
    .collection('homework')
    .where('teacherId', isEqualTo: user?.uid)
    .orderBy('createdAt', descending: true)
    .snapshots()
```

### **عرض الواجب:**
```dart
ListTile(
  leading: Icon(Icons.assignment),
  title: Text(data['title']),
  subtitle: Column(
    children: [
      Text('الصف: ${data['grade']}'),
      Text('الشعب: ${sections.join(', ')}'),
      Text(dateStr, style: TextStyle(fontSize: 11, color: grey)),
    ],
  ),
  trailing: IconButton(
    icon: Icon(Icons.delete, color: Colors.red),
    onPressed: () => _deleteHomework(doc),
  ),
)
```

### **حذف الواجب:**
```dart
final confirm = await showDialog<bool>(
  context: context,
  builder: (context) => AlertDialog(
    title: Text('تأكيد الحذف'),
    content: Text('هل تريد حذف هذا الواجب؟'),
    actions: [
      TextButton(onPressed: () => Navigator.pop(context, false), child: Text('إلغاء')),
      TextButton(onPressed: () => Navigator.pop(context, true), child: Text('حذف')),
    ],
  ),
);

if (confirm == true) {
  await doc.reference.delete();
  ScaffoldMessenger.of(context).showSnackBar(
    SnackBar(content: Text('تم حذف الواجب')),
  );
}
```

---

## 📁 **الملفات المعدلة:**

```
✅ lib/ui/teacher/teacher_home_complete.dart
   - إضافة TabController
   - إضافة TabBar
   - إضافة _buildSendHomeworkTab()
   - إضافة _buildSentHomeworkTab()
   - دعم حذف الواجبات

✅ توثيقات_المشروع/18_TEACHER_SENT_HOMEWORK_TAB.md
   - توثيق شامل
```

---

## 🧪 **الاختبار:**

### **1. اختبر عرض الواجبات:**
```
1. سجل دخول كمعلم
2. أرسل عدة واجبات
3. اذهب إلى تبويب "الواجبات المرسلة"
4. ✅ يجب أن تظهر جميع الواجبات
5. ✅ مرتبة من الأحدث للأقدم
```

### **2. اختبر حذف واجب:**
```
1. اضغط على زر الحذف
2. ✅ تظهر رسالة تأكيد
3. اضغط "حذف"
4. ✅ يتم حذف الواجب
5. ✅ تظهر رسالة "تم حذف الواجب"
6. ✅ يختفي الواجب من القائمة
```

### **3. اختبر حالة فارغة:**
```
1. احذف جميع الواجبات
2. ✅ تظهر رسالة "لا توجد واجبات مرسلة"
3. ✅ أيقونة واجب كبيرة
```

---

## 🎨 **التصميم:**

### **بطاقة الواجب:**
```
┌─────────────────────────────────┐
│  📚  واجب الدرس الأول           │
│      الصف: الثاني متوسط         │
│      الشعب: أ، ب                │
│      2025/10/31                  │  [🗑️]
└─────────────────────────────────┘
```

### **الألوان:**
```
- الخلفية: أبيض
- الأيقونة: أزرق فاتح
- العنوان: أسود غامق
- التفاصيل: رمادي
- التاريخ: رمادي فاتح (حجم 11)
- زر الحذف: أحمر
```

---

## 💡 **ملاحظات:**

### **StreamBuilder:**
```
- تحديث تلقائي عند إضافة/حذف واجب
- لا حاجة لإعادة تحميل الصفحة
- يعمل في الوقت الفعلي
```

### **الأداء:**
```
- Query محدد بـ teacherId
- ترتيب في Firestore (orderBy)
- عرض فوري للتغييرات
```

### **الأمان:**
```
- فقط المعلم يرى واجباته
- فقط المعلم يمكنه حذف واجباته
- تأكيد قبل الحذف
```

---

## 📊 **الإحصائيات:**

```
عدد التبويبات: 2
- إرسال واجب
- الواجبات المرسلة

الميزات:
✅ عرض الواجبات
✅ حذف الواجب
✅ تأكيد الحذف
✅ تحديث تلقائي
✅ عرض التاريخ
✅ حالة فارغة
```

---

**الحالة:** ✅ مكتمل ويعمل  
**جاهز للاختبار:** نعم  
**التحسينات:** تبويب احترافي + حذف سهل
