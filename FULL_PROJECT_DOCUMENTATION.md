# 🏫 نظام إدارة المدرسة - توثيق شامل

## 📘 مقدمة

### نظرة عامة
نظام إدارة مدرسية شامل يربط بين الإدارة والمعلمين والطلاب، مبني بتقنيات حديثة لتسهيل العملية التعليمية.

### الهدف
- إدارة فعّالة للمعلمين والطلاب
- تسهيل عملية إنشاء وتوزيع الواجبات
- تتبع الأداء الأكاديمي
- تواصل سلس بين الأطراف

### التقنيات المستخدمة
| التقنية | الاستخدام |
|---------|-----------|
| **Flutter** | Framework الواجهات (Android, iOS, Windows) |
| **Firebase Authentication** | نظام تسجيل الدخول والأمان |
| **Cloud Firestore** | قاعدة بيانات NoSQL |
| **Firebase Cloud Messaging** | نظام الإشعارات |
| **Firebase Cloud Functions** | عمليات الخادم |
| **GetX** | إدارة الحالة والتنقل |

### المنصات المدعومة
- ✅ Android
- ✅ iOS
- ✅ Windows
- 🔄 Web (قيد التطوير)

---

## 🧩 بنية المشروع (Project Structure)

```
madrasah/
├── lib/
│   ├── ui/                          # واجهات المستخدم
│   │   ├── admin/                   # شاشات الإدارة
│   │   │   ├── admin_tabs_screen.dart
│   │   │   ├── create_teacher_screen.dart
│   │   │   ├── edit_teacher_dialog.dart
│   │   │   ├── teachers_list_screen.dart
│   │   │   ├── students_list_screen.dart
│   │   │   ├── fix_teachers_button.dart
│   │   │   ├── fix_old_subjects_button.dart
│   │   │   └── setup_subjects_button.dart
│   │   │
│   │   ├── teacher/                 # شاشات المعلم
│   │   │   ├── home_screen.dart     # الصفحة الرئيسية + إنشاء واجب
│   │   │   ├── homework_list_screen.dart
│   │   │   └── profile_screen.dart
│   │   │
│   │   ├── student/                 # شاشات الطالب
│   │   │   ├── home_screen.dart
│   │   │   ├── homework_view_screen.dart
│   │   │   └── grades_screen.dart
│   │   │
│   │   ├── notifications/           # الإشعارات
│   │   │   └── inbox_screen.dart
│   │   │
│   │   └── widgets/                 # مكونات مشتركة
│   │       └── subject_3d_icons.dart
│   │
│   ├── models/                      # نماذج البيانات
│   │   ├── teacher_model.dart
│   │   ├── student_model.dart
│   │   ├── subject_model.dart
│   │   └── homework_model.dart
│   │
│   ├── services/                    # الخدمات
│   │   ├── teacher_setup_service.dart
│   │   ├── subjects_cache_service.dart
│   │   ├── analytics_service.dart
│   │   └── notification_service.dart
│   │
│   ├── utils/                       # أدوات مساعدة
│   │   ├── setup_subjects_with_metadata.dart
│   │   ├── add_subjects_quick.dart
│   │   ├── fix_teacher_missing_data.dart
│   │   ├── fix_old_subjects.dart
│   │   ├── diagnose_teacher.dart
│   │   └── quick_fix_widget.dart (محذوف بعد الإنتاج)
│   │
│   ├── constants/                   # الثوابت
│   │   └── education_constants.dart
│   │
│   └── routes/                      # التنقل
│       └── app_pages.dart
│
├── test/                            # الاختبارات
├── scripts/                         # سكربتات مساعدة
├── docs/                            # التوثيق
│   ├── FULL_PROJECT_DOCUMENTATION.md (هذا الملف)
│   ├── CHANGELOG.md
│   ├── FILES_INDEX.md
│   ├── API_REFERENCE.md
│   └── DEPLOYMENT_GUIDE.md
│
└── assets/                          # الموارد (صور، أيقونات)
```

---

## 🗂️ Firestore Database Structure

### Collections

#### 1. `users_emails` (المستخدمون)
```json
{
  "email@example.com": {
    "uid": "unique_id",
    "name": "اسم المستخدم",
    "email": "email@example.com",
    "role": "teacher|student|admin",
    "stage": "ابتدائية|متوسطة|إعدادية",
    "grade": "الأول|الثاني|...",
    "branch": "علمي|أدبي",  // للإعدادية فقط
    "sections": ["أ", "ب", "ج"],
    "subjects": ["subject_id1", "subject_id2"],  // للمعلمين
    "teacherId": "teacher_doc_id",  // مرجع للمعلم
    "isActive": true,
    "createdAt": "timestamp",
    "updatedAt": "timestamp"
  }
}
```

#### 2. `teachers` (المعلمون)
```json
{
  "teacher_id": {
    "name": "اسم المعلم",
    "email": "teacher@example.com",
    "stage": "إعدادية",
    "grade": "السادس",
    "branch": "علمي",
    "sections": ["أ", "ب"],
    "subjects": ["sec_6_physics_sci", "sec_6_chemistry_sci"],
    "isActive": true,
    "createdAt": "timestamp",
    "updatedAt": "timestamp"
  }
}
```

#### 3. `subjects` (المواد الدراسية)
```json
{
  "sec_6_physics_sci": {
    "name": "الفيزياء",
    "emoji": "⚛️",
    "stage": "إعدادية",
    "grade": "السادس",
    "branch": "علمي",
    "order": 5,
    "isActive": true,
    "createdAt": "timestamp"
  }
}
```

#### 4. `students` (الطلاب)
```json
{
  "student_id": {
    "name": "اسم الطالب",
    "email": "student@example.com",
    "stage": "إعدادية",
    "grade": "السادس",
    "section": "أ",
    "branch": "علمي",
    "parentPhone": "+964...",
    "isActive": true,
    "createdAt": "timestamp"
  }
}
```

#### 5. `homeworks` (الواجبات)
```json
{
  "homework_id": {
    "title": "واجب الفيزياء - الحركة",
    "description": "حل المسائل من 1-10",
    "teacherId": "teacher_id",
    "teacherName": "اسم المعلم",
    "subjectId": "sec_6_physics_sci",
    "subjectName": "الفيزياء",
    "stage": "إعدادية",
    "grade": "السادس",
    "sections": ["أ", "ب"],
    "dueDate": "timestamp",
    "createdAt": "timestamp",
    "status": "active|completed|expired"
  }
}
```

#### 6. `sections` (الشعب)
```json
{
  "section_id": {
    "name": "أ",
    "stage": "إعدادية",
    "grade": "السادس",
    "capacity": 30,
    "currentStudents": 25
  }
}
```

---

## 🕓 التسلسل الزمني للتطوير

### المرحلة 1: الإعداد الأولي (أكتوبر 2025)
- ✅ إنشاء مشروع Flutter
- ✅ ربط Firebase (Auth + Firestore)
- ✅ إعداد بنية المشروع
- ✅ إنشاء نماذج البيانات الأساسية

### المرحلة 2: واجهات الإدارة (أكتوبر 2025)
- ✅ شاشة تسجيل الدخول
- ✅ لوحة تحكم الإدارة
- ✅ إنشاء حساب معلم
- ✅ قائمة المعلمين
- ✅ تعديل بيانات المعلم

### المرحلة 3: واجهات المعلم (أكتوبر 2025)
- ✅ الصفحة الرئيسية
- ✅ إنشاء واجب
- ✅ قائمة الواجبات
- ✅ الإشعارات

### المرحلة 4: إصلاح المواد والشعب (أكتوبر 25-26، 2025)
- ✅ تشخيص المشكلة: المواد لا تظهر
- ✅ إنشاء سكربت إضافة المواد
- ✅ تحديث `edit_teacher_dialog.dart` للتحميل الديناميكي
- ✅ إضافة 132 مادة مع metadata
- ✅ اختبار شامل وتأكيد النجاح

### المرحلة 5: التنظيف والإنتاج (أكتوبر 26، 2025)
- ✅ حذف أدوات التشخيص المؤقتة
- ✅ تنظيف الكود
- ✅ إنشاء التوثيق الشامل
- 🔄 الاختبارات النهائية

---

## 🧠 سجل الأخطاء والإصلاحات

### E001: المواد والشعب لا تظهر في واجهة المعلم

**التاريخ:** 2025-10-25

**الملف المتأثر:** `lib/ui/teacher/home_screen.dart`

**الوصف:**
عند فتح صفحة المعلم، لا تظهر المواد في قائمة إنشاء الواجب.

**السبب:**
- المواد في Firestore لا تحتوي على حقول `stage`, `grade`, `branch`
- لا توجد آلية لفلترة المواد حسب الصف

**الحل:**
1. إضافة metadata لجميع المواد في Firestore
2. تحديث دالة `_loadTeacherSubjects()` للفلترة حسب الصف
3. إنشاء سكربت `setup_subjects_with_metadata.dart`

**الكود:**
```dart
// قبل
final snapshot = await FirebaseFirestore.instance
    .collection('subjects')
    .get(); // جميع المواد!

// بعد
Query query = FirebaseFirestore.instance.collection('subjects');
query = query.where('stage', isEqualTo: stage);
query = query.where('grade', isEqualTo: grade);
final snapshot = await query.get(); // المواد الصحيحة فقط
```

---

### E002: عدم حفظ sections و subjects عند إنشاء معلم

**التاريخ:** 2025-10-25

**الملف المتأثر:** `lib/ui/admin/create_teacher_screen.dart`

**الوصف:**
عند إنشاء معلم جديد، الحقول `sections` و `subjects` لا تُحفظ في Firestore.

**السبب:**
الكود القديم لم يكن يجمع هذه البيانات من الواجهة.

**الحل:**
إضافة UI elements وجمع البيانات قبل الحفظ:
```dart
final teacherData = {
  'name': nameController.text,
  'email': email,
  'stage': selectedStage,
  'grade': selectedGrade,
  'sections': selectedSections,  // ✅ مضاف
  'subjects': selectedSubjects,  // ✅ مضاف
  // ...
};
```

---

### E003: المواد القديمة تظهر بدلاً من الجديدة

**التاريخ:** 2025-10-25

**الملف المتأثر:** `lib/ui/admin/edit_teacher_dialog.dart`

**الوصف:**
عند تعديل معلم، تظهر المواد القديمة (من cache) بدلاً من المواد الجديدة حسب الصف.

**السبب:**
```dart
// الكود القديم كان يأخذ المواد من البيانات القديمة
final dynamic subjectsData = widget.teacher['subjects'];
if (subjectsData is List) {
  _selectedSubjects = subjectsData.cast<String>();
}
```

**الحل:**
```dart
// تجاهل المواد القديمة تماماً
_selectedSubjects = []; // قائمة فارغة

// تحميل المواد ديناميكياً حسب الصف
Future<void> _loadAvailableSubjects() async {
  Query query = FirebaseFirestore.instance.collection('subjects');
  query = query.where('stage', isEqualTo: _selectedStage);
  query = query.where('grade', isEqualTo: _selectedGrade);
  // ...
}
```

**إعادة التحميل عند التغيير:**
```dart
onChanged: (value) {
  setState(() {
    _selectedGrade = value;
  });
  _loadAvailableSubjects(); // ✅ إعادة تحميل فوري
}
```

---

## ⚙️ السكربتات والأدوات

### 1. `setup_subjects_with_metadata.dart`

**الوظيفة:** إضافة جميع المواد الدراسية (132 مادة) مع metadata كاملة

**الاستخدام:** مرة واحدة عند إعداد قاعدة البيانات

**المواد المضافة:**
- ابتدائية: 48 مادة (6 صفوف × 8 مواد)
- متوسطة: 27 مادة (3 صفوف × 9 مواد)
- إعدادية علمي: 27 مادة (3 صفوف × 9 مواد)
- إعدادية أدبي: 30 مادة (3 صفوف × 10 مواد)

**كيفية التشغيل:**
```dart
await SetupSubjectsWithMetadata.setupAll();
```

---

### 2. `add_subjects_quick.dart`

**الوظيفة:** إضافة سريعة لمواد الإعدادية - علمي (27 مادة)

**الاستخدام:** للاختبارات والإضافة السريعة

**المواد:**
- الصف الرابع: 9 مواد
- الصف الخامس: 9 مواد
- الصف السادس: 9 مواد

**كيفية التشغيل:**
```dart
await AddSubjectsQuick.addSecondaryScientific();
```

---

### 3. `fix_teacher_missing_data.dart`

**الوظيفة:** إصلاح بيانات المعلمين القدامى (إضافة sections و subjects)

**متى يُستخدم:** عندما تكون حسابات المعلمين القديمة تفتقد هذه الحقول

**ما يفعله:**
1. يفحص جميع المعلمين في `users_emails`
2. يضيف `sections: ['أ', 'ب', 'ج']` للذين يفتقدونها
3. يضيف `subjects: []` للذين يفتقدونها

**كيفية التشغيل:**
```dart
final result = await FixTeacherMissingData.fix();
```

---

### 4. `fix_old_subjects.dart`

**الوظيفة:** إصلاح المواد القديمة للمعلمين (تحديث حسب الصف الحالي)

**متى يُستخدم:** بعد إضافة المواد الجديدة، لتحديث حسابات المعلمين القديمة

**ما يفعله:**
1. يقرأ المرحلة والصف لكل معلم
2. يجلب المواد الصحيحة من Firestore
3. يستبدل المواد القديمة بالجديدة

**كيفية التشغيل:**
```dart
final result = await FixOldSubjects.fixAllTeachers();
```

---

### 5. `diagnose_teacher.dart`

**الوظيفة:** تشخيص سريع لبيانات المعلم الحالي

**متى يُستخدم:** لاستكشاف الأخطاء والتحقق من البيانات

**ما يطبعه:**
- UID والبريد الإلكتروني
- المرحلة والصف والفرع
- الشعب
- المواد (IDs وعددها)
- التحقق من صحة البيانات

**كيفية التشغيل:**
```dart
await DiagnoseTeacher.check();
```

---

### 6. `quick_fix_widget.dart` ⚠️ (مؤقت - محذوف في الإنتاج)

**الوظيفة:** أداة UI للتشخيص والإصلاح السريع

**الأزرار:**
1. تشخيص المشكلة
2. إصلاح جميع المعلمين
3. إصلاح المعلم الحالي
4. إضافة سريعة للمواد
5. إعداد كامل للمواد

**متى يُستخدم:** أثناء التطوير فقط

**حذفه:** بعد التأكد من نجاح كل شيء

---

## 🧪 الاختبارات

### اختبارات الإدارة

#### 1. إنشاء معلم جديد
```
✅ الخطوات:
1. تسجيل دخول كإدارة
2. اذهب إلى "المعلمون"
3. اضغط "إنشاء معلم جديد"
4. املأ البيانات (الاسم، البريد، المرحلة، الصف، الفرع)
5. اختر الشعب
6. اختر المواد (يجب أن تظهر حسب الصف!)
7. احفظ

✅ النتيجة المتوقعة:
- المعلم يُنشأ في users_emails و teachers
- البيانات تُحفظ بشكل صحيح
- المواد والشعب موجودة
```

#### 2. تعديل معلم موجود
```
✅ الخطوات:
1. اختر معلم من القائمة
2. اضغط "تعديل"
3. غيّر الصف
4. لاحظ: المواد تتحدث فوراً!
5. اختر مواد جديدة
6. احفظ

✅ النتيجة المتوقعة:
- المواد تُحدّث في Firestore
- المواد القديمة تُحذف (استبدال كامل)
```

---

### اختبارات المعلم

#### 1. عرض المواد والشعب
```
✅ الخطوات:
1. تسجيل دخول كمعلم
2. افتح الصفحة الرئيسية
3. تحقق من عرض المواد في البطاقات
4. تحقق من عرض الشعب

✅ النتيجة المتوقعة:
- المواد تظهر بشكل صحيح
- الإيموجي والأسماء صحيحة
```

#### 2. إنشاء واجب
```
✅ الخطوات:
1. اذهب إلى "إنشاء واجب"
2. تحقق من القائمة المنسدلة للمواد
3. تحقق من اختيار الشعب
4. املأ العنوان والوصف
5. اختر تاريخ التسليم
6. أرسل

✅ النتيجة المتوقعة:
- المواد تظهر (المواد المخصصة فقط)
- الشعب تظهر
- الواجب يُنشأ في Firestore
- الطلاب يتلقون إشعار
```

---

### اختبارات الطالب

#### 1. عرض الواجبات
```
✅ الخطوات:
1. تسجيل دخول كطالب
2. عرض قائمة الواجبات
3. تحقق من الفلترة حسب المادة
4. افتح واجب محدد

✅ النتيجة المتوقعة:
- الواجبات تظهر حسب الصف والشعبة
- التفاصيل صحيحة
```

---

## 🚀 دليل النشر (Deployment)

### المتطلبات
- Flutter SDK (>= 3.0.0)
- Firebase project مُجهّز
- Android Studio / Xcode (للبناء)

### خطوات النشر

#### 1. إعداد Firebase
```bash
# تثبيت Firebase CLI
npm install -g firebase-tools

# تسجيل الدخول
firebase login

# ربط المشروع
flutterfire configure
```

#### 2. بناء التطبيق

**Android:**
```bash
flutter build apk --release
# أو
flutter build appbundle --release
```

**iOS:**
```bash
flutter build ios --release
```

**Windows:**
```bash
flutter build windows --release
```

#### 3. رفع على Google Play / App Store
- اتبع دليل النشر الرسمي لكل منصة

---

## 📊 إحصائيات المشروع

| المقياس | القيمة |
|---------|--------|
| **عدد الملفات** | ~50 ملف |
| **عدد الأسطر** | ~15,000 سطر |
| **عدد المواد** | 132 مادة |
| **عدد المراحل** | 3 (ابتدائية، متوسطة، إعدادية) |
| **عدد الصفوف** | 12 صف |
| **Collections في Firestore** | 6 |

---

## 🔮 خطط مستقبلية

### قريباً
- [ ] شاشة تقارير الواجبات
- [ ] إحصائيات أداء الطلاب
- [ ] رفع ملفات PDF في الواجبات
- [ ] نظام التقييم والدرجات

### متوسط المدى
- [ ] دعم Web بشكل كامل
- [ ] تطبيق للأهالي
- [ ] نظام الحضور والغياب
- [ ] جدول الحصص

### طويل المدى
- [ ] التكامل مع أنظمة المدارس الحكومية
- [ ] تقارير تحليلية متقدمة (AI)
- [ ] دعم اللغات المتعددة
- [ ] وضع Offline

---

## 📞 الدعم والتواصل

للأسئلة والمساعدة:
- GitHub Issues
- البريد الإلكتروني: [email]
- التوثيق: `/docs`

---

**آخر تحديث:** 2025-10-26  
**الإصدار:** 1.0.0  
**الحالة:** ✅ جاهز للإنتاج
