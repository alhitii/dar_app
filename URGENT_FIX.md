# 🚨 إصلاح عاجل - بيانات الطالب

## المشكلة:
```
UID: bwsnflnliggjM75U5EBIuvoBTwU2
Document exists: false ❌
```

البيانات موجودة في `users` لكن غير موجودة في `students`

---

## ✅ الحل السريع (من Firebase Console):

### الخطوة 1: نسخ البيانات
```
1. افتح Firebase Console
2. اذهب إلى Firestore Database
3. افتح collection: users
4. ابحث عن document: bwsnflnliggjM75U5EBIuvoBTwU2
5. انسخ جميع الحقول (name, email, grade, section, stage, role)
```

### الخطوة 2: إنشاء Document جديد
```
1. في Firestore، اذهب إلى collection: students
2. اضغط "Add document"
3. Document ID: bwsnflnliggjM75U5EBIuvoBTwU2
4. الصق الحقول التي نسختها:
   - name (string): "..."
   - email (string): "...@codeira.com"
   - grade (string): "..."
   - section (string): "..."
   - stage (string): "..."
   - role (string): "student"
5. اضغط Save
```

### الخطوة 3: اختبر
```
1. أعد تشغيل التطبيق
2. سجل دخول
3. ✅ يجب أن تظهر جميع البيانات
```

---

## 🔧 الحل البديل (إعادة إنشاء الحساب):

```
1. من واجهة الإدارة في التطبيق
2. احذف الحساب الحالي
3. أنشئ حساب جديد بنفس البيانات
4. ✅ سيتم حفظ البيانات في كلا المكانين تلقائياً
```

---

## 📊 البيانات المطلوبة:

```json
{
  "name": "اسم الطالب",
  "email": "username@codeira.com",
  "username": "username",
  "role": "student",
  "stage": "متوسطة أو إعدادية",
  "grade": "الأول متوسط، الثاني متوسط، إلخ",
  "section": "أ، ب، ج، إلخ",
  "createdAt": "timestamp"
}
```

---

## ⚠️ ملاحظة:

الأخطاء الأخرى في Console:
```
[ERROR:flutter/shell/common/shell.cc(1120)] The 'plugins.flutter.io/firebase_firestore...
```

هذه تحذيرات من Firebase plugin ولا تؤثر على عمل التطبيق.
المشكلة الحقيقية هي: `Document exists: false`

---

## ✅ بعد الإصلاح:

```
Console سيظهر:
=== تحميل بيانات الطالب ===
UID: bwsnflnliggjM75U5EBIuvoBTwU2
Document exists: true  ✅
Data: {name: ..., grade: ..., ...}
```

---

**اختر أحد الحلين وسيعمل التطبيق فوراً! 🚀**
