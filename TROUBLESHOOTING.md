# 🔧 دليل استكشاف الأخطاء

## ❌ المشكلة: البانر لا يظهر

### السبب الأساسي:
**لا توجد بيانات في Firebase!**

---

## ✅ الحل السريع (3 خطوات):

### الخطوة 1: تفعيل وضع التصحيح

تم إضافة `DebugBannersWidget` في الصفحة الرئيسية للطالب.

عند فتح التطبيق، سترى صندوق أصفر يعرض:
- عدد الإعلانات الموجودة
- عدد إشعارات الغياب
- أزرار لإضافة بيانات تجريبية

```
┌─────────────────────────────────┐
│ 🔧 وضع التصحيح                 │
│                                 │
│ User UID: abc123                │
│                                 │
│ 📢 Announcements: 0             │
│   ⚠️ لا توجد إعلانات!           │
│                                 │
│ ⚠️ Absences: 0                  │
│   ⚠️ لا توجد إشعارات غياب!     │
│                                 │
│ [إضافة إعلان] [إضافة غياب]     │
└─────────────────────────────────┘
```

### الخطوة 2: إضافة بيانات تجريبية

**من داخل التطبيق:**
1. افتح التطبيق كطالب
2. في صندوق التصحيح، اضغط:
   - **"إضافة إعلان تجريبي"** ← سيضيف إعلان
   - **"إضافة غياب تجريبي"** ← سيضيف إشعار غياب
3. **سيظهر البانر فوراً!** ✨

**من Firebase Console:**
1. افتح Firebase Console
2. Firestore Database
3. أضف Collection: `announcements`
4. أضف Document:

```json
{
  "title": "موعد الاختبار",
  "message": "تذكير بموعد الاختبار",
  "type": "info",
  "targetRole": "student",
  "createdAt": [Server Timestamp]
}
```

### الخطوة 3: التحقق

بعد إضافة البيانات:
- سيتحدث صندوق التصحيح: `📢 Announcements: 1`
- سيظهر البانر أسفل صندوق التصحيح مباشرة ✅

---

## 🔍 فحص نقاط محددة

### 1. هل Firebase متصل؟

افتح **Firestore** في Firebase Console وتأكد من:
- ✅ قاعدة البيانات مفعّلة
- ✅ القواعد تسمح بالقراءة

```javascript
// قواعد للاختبار (ليست آمنة للإنتاج!)
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;  // للاختبار فقط!
    }
  }
}
```

### 2. هل الطالب مسجل دخول؟

في صندوق التصحيح، تأكد من:
```
User UID: abc123  ← يجب أن يكون موجود
```

إذا كان `User UID: غير مسجل` ← سجل دخول أولاً!

### 3. هل البيانات موجهة للطالب؟

في document الإعلان، تأكد من:
```javascript
{
  "targetRole": "student"  // أو "all"
}
```

❌ إذا كان `"targetRole": "teacher"` لن يظهر للطلاب!

---

## 📊 رسائل الخطأ الشائعة

### خطأ: "لا توجد بيانات"
**السبب:** Collection فارغ
**الحل:** أضف بيانات تجريبية من صندوق التصحيح

### خطأ: "Permission denied"
**السبب:** قواعد Firebase
**الحل:** راجع قواعد Firestore

### خطأ: "User UID: غير مسجل"
**السبب:** لم يتم تسجيل الدخول
**الحل:** سجل دخول بحساب طالب

---

## 🎯 خطوات التأكد الكاملة

### ✅ قائمة التحقق:

1. **Firebase متصل؟**
   - [ ] قاعدة البيانات مفعلة
   - [ ] القواعد تسمح بالقراءة

2. **البيانات موجودة؟**
   - [ ] Collection `announcements` موجود
   - [ ] يحتوي على documents
   - [ ] `targetRole` صحيح

3. **الطالب مسجل؟**
   - [ ] User UID موجود
   - [ ] الحساب نشط

4. **الكود صحيح؟**
   - [ ] `AdminAnnouncementsBanner` مضاف
   - [ ] `AbsenceBanner` مضاف
   - [ ] Imports صحيحة

---

## 🚀 الحل النهائي

### إذا لم ينجح أي شيء:

1. **أعد تشغيل التطبيق**
   ```bash
   flutter clean
   flutter pub get
   flutter run
   ```

2. **استخدم صندوق التصحيح**
   - افتح التطبيق
   - اضغط "إضافة إعلان تجريبي"
   - يجب أن يظهر فوراً!

3. **تحقق من Console**
   - افتح Debug Console في VS Code
   - ابحث عن أخطاء Firebase

---

## 📝 ملاحظات

### بعد حل المشكلة:

**احذف widget التصحيح:**
```dart
// في student/home_screen.dart
// احذف هذا السطر:
DebugBannersWidget(),  // ← احذف هذا

// واحذف import:
import '../widgets/debug_banners.dart';  // ← احذف هذا
```

### للإنتاج:

1. احذف `DebugBannersWidget`
2. حدّث قواعد Firebase لتكون آمنة
3. أضف بيانات حقيقية

---

## 🎉 النتيجة المتوقعة

بعد إضافة البيانات:

```
┌─────────────────────────────────┐
│ 🔧 وضع التصحيح                 │
│ 📢 Announcements: 1             │
│   • موعد الاختبار (student)    │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ ℹ️ موعد الاختبار  منذ دقيقة   │
│ تذكير بموعد...              → │
└─────────────────────────────────┘
```

**✨ يجب أن يظهر البانر!**

---

## ❓ الأسئلة الشائعة

### س: لماذا لا يظهر البانر حتى بعد إضافة البيانات؟
**ج:** تأكد من:
1. `targetRole` صحيح (`student` أو `all`)
2. `createdAt` موجود وهو `Server Timestamp`
3. لم يمر 24 ساعة على الإنشاء

### س: كيف أتأكد أن البيانات موجودة؟
**ج:** افتح Firebase Console → Firestore → شاهد البيانات مباشرة

### س: هل يجب تشغيل سكريبت؟
**ج:** لا! استخدم الأزرار في صندوق التصحيح أسهل وأسرع

---

## 📞 إذا احتجت مساعدة

1. شغّل التطبيق
2. التقط صورة لصندوق التصحيح
3. التقط صورة من Firebase Console
4. شارك الصور

**🎊 بالتوفيق!**
