# 🧪 دليل اختبار الإصلاحات النهائية

## 📋 **اختبار FCM Token:**

### **الخطوات:**
1. **احذف التطبيق** من الهاتف تماماً
2. **بناء APK جديد:**
   ```bash
   flutter clean
   flutter pub get
   flutter build apk --release
   ```
3. **تثبيت APK الجديد** على الهاتف
4. **تشغيل التطبيق مع Terminal:**
   ```bash
   flutter run
   ```
5. **تسجيل دخول** كطالب أو معلم

### **التحقق من النجاح:**
```bash
# في Terminal يجب أن تظهر:
🔑 FCM Token: eFv9k3tn...
✅ تم حفظ FCM Token في Firestore للمستخدم: [userId]
```

### **التحقق من Firestore:**
- اذهب إلى Firebase Console
- Collections → users → [userId]
- ابحث عن: `fcmToken`
- يجب أن يكون موجوداً

---

## 📋 **اختبار الإشعارات:**

### **الخطوات:**
1. **سجل دخول كطالب** على الهاتف الأول
2. **سجل دخول كمعلم** على الهاتف الثاني
3. **أضف واجب جديد** من المعلم
4. **انتظر 5-10 ثواني**
5. **تحقق من وصول الإشعار** للطالب

### **التحقق من إعدادات الهاتف:**
```bash
Settings → Apps → [التطبيق] → Notifications
✅ Allow notifications: ON
✅ Sound: ON
✅ Vibration: ON
```

---

## 📋 **اختبار البيانات الأساسية:**

### **التحقق من البيانات:**
```bash
# في Firebase Console
Collections → classes    # يجب أن تحتوي على 12 صف
Collections → sections   # يجب أن تحتوي على 6 شعب
Collections → subjects   # يجب أن تحتوي على 132 مادة
```

---

## 📋 **سيناريوهات الاختبار:**

### **✅ سيناريو ناجح:**
```
1. تسجيل الدخول: ✅
   - Terminal: 🔑 FCM Token موجود
   - Firestore: fcmToken محفوظ

2. إرسال إشعار: ✅
   - صوت واضح
   - اهتزاز قوي
   - إشعار مرئي

3. البيانات: ✅
   - جميع الصفوف موجودة
   - جميع الشعب موجودة
   - جميع المواد موجودة
```

### **❌ سيناريو فاشل:**
```
1. لا يظهر FCM Token → تحقق من الأذونات
2. لا يوجد صوت → تحقق من إعدادات الهاتف
3. البيانات فارغة → أعد تشغيل التطبيق
```

---

## 📋 **أوامر الاختبار:**

```bash
# 1. تحضير البيئة
flutter clean
flutter pub get

# 2. بناء APK للإنتاج
flutter build apk --release

# 3. تثبيت على الهاتف
adb install build/app/outputs/flutter-apk/app-release.apk

# 4. تشغيل مع السجلات
flutter run --debug

# 5. تسجيل الدخول ومراقبة السجلات
```

---

## 📋 **نقاط التحقق:**

- [ ] FCM Token يظهر في Terminal
- [ ] FCM Token محفوظ في Firestore
- [ ] الإشعارات تصل مع الصوت
- [ ] البيانات الأساسية موجودة
- [ ] لا توجد أخطاء في السجلات
- [ ] التطبيق يعمل بسلاسة

---

## 🎯 **النتيجة المتوقعة:**

**إذا نجح جميع الاختبارات:**
✅ **المشروع جاهز 100% للإنتاج!** 🚀

**إذا فشل بعض الاختبارات:**
🔧 **تحتاج إصلاحات إضافية** (أخبرني بالتفاصيل)

---

**ابدأ الاختبار الآن!** 🧪
