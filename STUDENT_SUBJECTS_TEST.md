# اختبار شامل لإصلاح عرض المواد للطالب

## ✅ التحديثات المطبقة

### 1. إنشاء SubjectsProvider موحد
- **الملف**: `lib/services/subjects_provider.dart`
- **الوظيفة**: يعيد المواد حسب المرحلة والصف والفرع من EducationConstants
- **الميزات**:
  - `getSubjects(stage, grade, branch)`: جلب المواد المناسبة
  - `isValidStudentData()`: التحقق من صحة البيانات
  - `getLevelDescription()`: وصف المستوى التعليمي

### 2. إنشاء StudentDataService محسن
- **الملف**: `lib/services/student_data_service.dart`
- **الوظيفة**: جلب بيانات الطالب المحدثة من Firebase
- **الميزات**:
  - `getCurrentStudentData()`: جلب بيانات الطالب من Firebase
  - `getCurrentStudentSubjects()`: جلب المواد المحدثة
  - `watchCurrentStudent()`: مراقبة تغييرات بيانات الطالب
  - `needsSubjectsUpdate()`: فحص الحاجة لتحديث المواد

### 3. تحديث الصفحة الرئيسية للطالب
- **الملف**: `lib/ui/student/home_screen.dart`
- **التغييرات**:
  - ❌ إزالة: `FirebaseFirestore.instance.collection('subjects')`
  - ✅ إضافة: `StudentDataService().watchCurrentStudent()`
  - ✅ عرض معلومات الطالب المحدثة (المرحلة، الصف، الفرع)
  - ✅ عرض المواد حسب بيانات الطالب الفعلية
  - ✅ رسالة واضحة عند عدم وجود مواد

### 4. تحديث صفحة المواد للطالب
- **الملف**: `lib/ui/student/student_subjects_screen.dart`
- **التغييرات**:
  - ✅ استخدام `StudentDataService` بدلاً من `FirebaseUserService`
  - ✅ جلب المواد المحدثة من قاعدة البيانات

## 🧪 خطوات الاختبار

### اختبار 1: طالب ابتدائي
```
1. أنشئ طالب جديد:
   - المرحلة: ابتدائية
   - الصف: الثالث
   - الشعبة: أ
2. سجل دخول كطالب
3. تحقق من المواد المعروضة:
   - يجب أن تظهر مواد الصف الثالث الابتدائي فقط
   - لا تظهر مواد المراحل الأخرى
```

### اختبار 2: طالب متوسط
```
1. أنشئ طالب جديد:
   - المرحلة: متوسطة
   - الصف: الثاني
   - الشعبة: ب
2. سجل دخول كطالب
3. تحقق من المواد المعروضة:
   - يجب أن تظهر مواد الصف الثاني المتوسط فقط
```

### اختبار 3: طالب إعدادي علمي
```
1. أنشئ طالب جديد:
   - المرحلة: إعدادية
   - الصف: الخامس
   - الفرع: علمي
   - الشعبة: أ
2. سجل دخول كطالب
3. تحقق من المواد المعروضة:
   - يجب أن تظهر مواد الخامس الإعدادي العلمي
   - يجب أن تتضمن: الرياضيات، الفيزياء، الكيمياء، الأحياء
```

### اختبار 4: طالب إعدادي أدبي
```
1. أنشئ طالب جديد:
   - المرحلة: إعدادية
   - الصف: السادس
   - الفرع: أدبي
   - الشعبة: ج
2. سجل دخول كطالب
3. تحقق من المواد المعروضة:
   - يجب أن تظهر مواد السادس الإعدادي الأدبي
   - يجب أن تتضمن: التاريخ، الجغرافيا، الفلسفة
   - لا تتضمن: الفيزياء، الكيمياء، الأحياء
```

### اختبار 5: تحديث بيانات الطالب
```
1. أنشئ طالب في الصف الثالث الابتدائي
2. سجل دخول وتحقق من المواد
3. من الإدارة، عدّل بيانات الطالب:
   - غيّر المرحلة إلى: متوسطة
   - غيّر الصف إلى: الأول
4. سجل دخول مرة أخرى كطالب
5. تحقق من أن المواد تغيرت تلقائياً لمواد الأول المتوسط
```

### اختبار 6: تغيير الفرع (إعدادي)
```
1. أنشئ طالب إعدادي علمي
2. سجل دخول وتحقق من المواد العلمية
3. من الإدارة، غيّر الفرع إلى: أدبي
4. سجل دخول مرة أخرى كطالب
5. تحقق من أن المواد تغيرت من علمية إلى أدبية
```

## 🎯 النتائج المتوقعة

### ✅ ما يجب أن يعمل الآن:
- المواد تُجلب من قاعدة البيانات حسب بيانات الطالب الفعلية
- تحديث فوري للمواد عند تغيير بيانات الطالب
- عرض معلومات الطالب المحدثة (المرحلة، الصف، الفرع)
- لا عرض لمواد قديمة أو غير مناسبة
- رسائل واضحة عند عدم وجود مواد

### ❌ ما لا يجب أن يحدث:
- عرض مواد من مراحل أخرى
- عرض مواد قديمة بعد تحديث بيانات الطالب
- عرض مواد علمية لطالب أدبي أو العكس
- استخدام قوائم مواد ثابتة أو محلية

## 🔧 الملفات المحدثة

1. `lib/services/subjects_provider.dart` - جديد
2. `lib/services/student_data_service.dart` - جديد  
3. `lib/ui/student/home_screen.dart` - محدث
4. `lib/ui/student/student_subjects_screen.dart` - محدث

## 📝 ملاحظات إضافية

- جميع التحديثات تستخدم Firebase كمصدر البيانات الأساسي
- لا توجد قوائم مواد محلية أو ثابتة
- النظام يدعم جميع المراحل والصفوف والفروع
- التحديثات فورية عند تغيير بيانات الطالب
- معالجة أخطاء شاملة مع رسائل واضحة
