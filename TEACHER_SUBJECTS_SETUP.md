# 🔧 إعداد المواد للمعلم - دليل سريع

## 📊 المشكلة الحالية
**الرسالة:** "لا توجد مواد مخصصة لك بعد"
**السبب:** حساب المعلم في Firestore لا يحتوي على حقل `subjects`

---

## ✅ الحل السريع

### 1️⃣ في Firebase Console

#### **افتح Firestore Database**
```
Firebase Console → Firestore Database → collection: users
```

#### **اختر وثيقة المعلم**
ابحث عن الوثيقة بـ UID المعلم (يظهر في Console: `🔑 UID المعلم: xxx`)

#### **أضف حقل subjects**
```json
{
  "name": "أحمد المعلم",
  "email": "teacher@school.com",
  "role": "teacher",
  "subjects": ["subject_001", "subject_002", "subject_003"]
}
```

---

### 2️⃣ تحقق من collection subjects

تأكد من وجود المواد في `collection("subjects")`:

```
subjects/
  ├── subject_001/
  │   ├── name: "الرياضيات"
  │   ├── emoji: "🔢"
  │   └── ...
  ├── subject_002/
  │   ├── name: "العلوم"
  │   ├── emoji: "🔬"
  │   └── ...
  └── subject_003/
      ├── name: "اللغة العربية"
      ├── emoji: "📖"
      └── ...
```

---

### 3️⃣ مثال عملي كامل

#### **وثيقة المعلم:**
```json
{
  "uid": "teacher123",
  "name": "أحمد محمد",
  "email": "ahmed@school.com",
  "role": "teacher",
  "stage": "إعدادية",
  "grade": "الأول",
  "sections": ["أ", "ب"],
  "subjects": [
    "math_middle_1",
    "science_middle_1",
    "arabic_middle_1"
  ],
  "createdAt": "2025-01-20T10:00:00Z"
}
```

#### **وثائق المواد المطابقة:**
```
subjects/math_middle_1:
{
  "name": "رياضيات - أول إعدادي",
  "emoji": "🔢",
  "stage": "إعدادية",
  "grade": "الأول"
}

subjects/science_middle_1:
{
  "name": "علوم - أول إعدادي",
  "emoji": "🔬",
  "stage": "إعدادية",
  "grade": "الأول"
}

subjects/arabic_middle_1:
{
  "name": "لغة عربية - أول إعدادي",
  "emoji": "📖",
  "stage": "إعدادية",
  "grade": "الأول"
}
```

---

## 🧪 اختبار النتيجة

### **شغّل التطبيق:**
```bash
flutter run -d windows
```

### **راقب Console - يجب أن تظهر:**
```
🔑 UID المعلم: teacher123
📄 بيانات المستخدم: name, email, subjects, role, ...
🔢 عدد IDs المواد في بيانات المعلم: 3
📝 IDs المواد: math_middle_1, science_middle_1, arabic_middle_1
✅ تم العثور على 3 مادة في قاعدة البيانات
📚 تم تحميل 3 مادة للمعلم
📖 المواد: رياضيات - أول إعدادي, علوم - أول إعدادي, لغة عربية - أول إعدادي
```

### **في واجهة التطبيق:**
- ✅ قائمة المواد تظهر: **"اختر المادة (3 متاحة)"**
- ✅ يمكن النقر على القائمة
- ✅ تظهر المواد الثلاث عند الفتح
- ✅ عند الاختيار: `✅ تم اختيار المادة: math_middle_1`

---

## 🚨 تشخيص الأخطاء الشائعة

| الرسالة في Console | المشكلة | الحل |
|-------------------|---------|------|
| `❌ لم يتم العثور على UID` | المعلم غير مسجل دخول | سجّل دخول بحساب المعلم |
| `❌ لم يتم العثور على بيانات في users` | وثيقة المعلم غير موجودة | أنشئ وثيقة في `users` collection |
| `⚠️ لا توجد مواد في حقل subjects` | حقل `subjects` مفقود أو فارغ | أضف `"subjects": [...]` للوثيقة |
| `🔢 عدد IDs المواد: 3` لكن `✅ تم العثور على 0` | IDs المواد غير موجودة في `subjects` collection | تأكد من مطابقة IDs |

---

## 💡 نصائح إضافية

### **استخدام Admin Panel**
إذا كان لديك صفحة إدارة، يمكنك:
1. إنشاء المواد أولاً
2. ثم إسناد المواد للمعلم عند إنشاء حسابه

### **Firestore Rules**
تأكد من أن Rules تسمح بقراءة المواد:
```javascript
match /subjects/{subjectId} {
  allow read: if request.auth != null;
}

match /users/{userId} {
  allow read: if request.auth != null && request.auth.uid == userId;
}
```

---

## ✅ الخلاصة

1. ✅ **الكود يعمل بشكل صحيح**
2. ✅ **المشكلة في البيانات فقط**
3. ✅ **أضف حقل `subjects` لحساب المعلم**
4. ✅ **تأكد من مطابقة IDs مع `subjects` collection**
5. ✅ **راقب Console للتحقق**

بعد إصلاح البيانات، القائمة ستعمل فوراً! 🚀
