# 🔧 إصلاح مشكلة عدم ظهور اسم المعلم

## ❓ **المشكلة:**
اسم المعلم لا يظهر تحت المادة في تبويبات المواد للطالب.

---

## 🔍 **السبب المحتمل:**

### **1. البيانات غير موجودة في `subjects` collection:**
```
المطلوب في كل document في subjects:
{
  "name": "الكيمياء",
  "grade": "الأول",
  "section": "أ",
  "teacherId": "xyz123",
  "teacherName": "محمد علي",  ← ✅ هذا المطلوب!
  ...
}
```

**إذا كان `teacherName` غير موجود أو `null`، لن يظهر الاسم!**

---

## ✅ **الحل:**

### **الطريقة 1: التحقق من البيانات في Firestore**

```
1. افتح Firebase Console
2. اذهب إلى Firestore Database
3. افتح collection: subjects
4. افتح أي document (مادة)
5. تحقق من وجود field: teacherName
6. إذا لم يكن موجوداً، أضفه يدوياً:
   - اسم الحقل: teacherName
   - النوع: string
   - القيمة: اسم المعلم (مثل: "محمد علي")
7. Save
```

---

### **الطريقة 2: إضافة teacherName تلقائياً**

**إذا كانت المواد موجودة لكن بدون `teacherName`:**

يمكن إضافة سكريبت في Cloud Functions أو في التطبيق لإضافة `teacherName` تلقائياً من `users` collection.

---

## 🧪 **كيفية الاختبار:**

### **1. افتح التطبيق:**
```bash
flutter run
```

### **2. سجّل دخول كطالب**

### **3. انظر إلى Terminal/Console:**
```
ابحث عن هذه الرسائل:
📚 معلومات المادة: الكيمياء
   Grade: الأول, Section: أ
   Teacher Name: محمد علي  ← ✅ يجب أن يظهر هنا
   Subject Data: {...}

أو:
⚠️ لم يتم العثور على معلومات المادة: الكيمياء
   Grade: الأول, Section: أ
   ← ❌ يعني الاستعلام لم يجد المادة
```

---

## 📊 **السيناريوهات المحتملة:**

### **سيناريو 1: teacherName موجود ✅**
```
Console Output:
📚 معلومات المادة: الكيمياء
   Teacher Name: محمد علي

النتيجة:
✅ يجب أن يظهر "الأستاذ: محمد علي" تحت المادة
```

---

### **سيناريو 2: teacherName = null ❌**
```
Console Output:
📚 معلومات المادة: الكيمياء
   Teacher Name: غير موجود
   Subject Data: {name: الكيمياء, grade: الأول, ...}

النتيجة:
❌ لن يظهر اسم المعلم

الحل:
أضف teacherName في Firestore
```

---

### **سيناريو 3: المادة غير موجودة في subjects ❌**
```
Console Output:
⚠️ لم يتم العثور على معلومات المادة: الكيمياء
   Grade: الأول, Section: أ

النتيجة:
❌ لن يظهر اسم المعلم

الحل:
تأكد من:
1. اسم المادة في subjects يطابق اسم المادة في users
2. الصف والشعبة يطابقان
```

---

## 🛠️ **إضافة teacherName يدوياً:**

### **من Firebase Console:**

```
1. Firestore Database → subjects
2. اختر مادة (مثل: الكيمياء - الأول - أ)
3. إذا لم يوجد teacherName:
   - اضغط "Add field"
   - Field name: teacherName
   - Type: string
   - Value: محمد علي (مثلاً)
   - Save
4. كرر لجميع المواد
```

---

### **من التطبيق (للإدارة):**

في المستقبل، عند إضافة معلم لمادة، يجب التأكد من حفظ اسمه في subjects:

```dart
await FirebaseFirestore.instance
    .collection('subjects')
    .doc(subjectId)
    .set({
  'teacherId': teacherId,
  'teacherName': teacherName,  ← ✅ احفظ الاسم أيضاً
  ...
}, SetOptions(merge: true));
```

---

## 📱 **الكود الحالي:**

```dart
// في student/home_screen.dart:

// جلب معلومات المادة
StreamBuilder<QuerySnapshot<Map<String, dynamic>>>(
  stream: FirebaseFirestore.instance
      .collection('subjects')
      .where('name', isEqualTo: subjectName)
      .where('grade', isEqualTo: grade)
      .where('section', isEqualTo: section)
      .limit(1)
      .snapshots(),
  builder: (context, subjectSnap) {
    String? teacherName;
    if (subjectSnap.hasData && subjectSnap.data!.docs.isNotEmpty) {
      teacherName = subjectSnap.data!.docs.first.data()['teacherName'];
      // ✅ إذا كان موجوداً، سيظهر
    }
    
    // عرض اسم المعلم
    if (teacherName != null && teacherName.isNotEmpty) ...[
      Text('الأستاذ: $teacherName'),  ← ✅ هنا يظهر
    ],
  }
)
```

---

## 🎯 **الخلاصة:**

```
السبب الأكثر احتمالاً:
❌ teacherName غير موجود في subjects collection

الحل:
1. افتح Firebase Console
2. اذهب إلى subjects
3. أضف field: teacherName لكل مادة
4. أعد تشغيل التطبيق
5. يجب أن يظهر الاسم ✅
```

---

## 📞 **بعد الاختبار:**

**أخبرني:**
1. ماذا يقول Console/Terminal؟
2. هل teacherName موجود في subjects؟
3. هل ظهر الاسم بعد إضافته؟

**وسأساعدك في الحل!** 🔧✨
